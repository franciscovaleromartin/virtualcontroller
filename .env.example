# ClickUp OAuth Configuration
CLICKUP_CLIENT_ID=your_clickup_client_id
CLICKUP_CLIENT_SECRET=your_clickup_client_secret

# URL de callback OAuth (IMPORTANTE: debe coincidir EXACTAMENTE con la registrada en ClickUp)
# Si no se especifica, se genera automáticamente. Ejemplos:
# - Desarrollo local: http://localhost:5000/ o http://localhost:5000
# - Producción: https://virtualcontroller.onrender.com o https://virtualcontroller.onrender.com/
# NOTA: Verifica si ClickUp requiere la URL con o sin "/" al final
REDIRECT_URI=https://virtualcontroller.onrender.com

# ClickUp API Token (para webhooks desde Make.com)
# Genera un Personal API Token en: https://app.clickup.com/settings/apps
# Este token permite que el webhook obtenga detalles de tareas desde la API cuando recibe datos incompletos
CLICKUP_API_TOKEN=pk_your_personal_api_token_here

# Email Configuration for Alerts (using Brevo API)
# Get your API Key at: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=your_brevo_api_key_here
SMTP_EMAIL=your_email@domain.com

# Webhook Security
# Generate a secure random token to validate webhook requests from make.com
WEBHOOK_SECRET_TOKEN=your_secure_random_token_here

# Database Configuration
# Path to SQLite database file (optional, defaults to virtualcontroller.db)
DATABASE_PATH=virtualcontroller.db

# Google OAuth Configuration (for Google Sheets Export)
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:5000/oauth/google/callback
